package me.donald

/**
 * 동일 패키지
 * - 동일 패키지에서는 별도의 import 문이 없다.
 * - 동일 패키지에서 선언된 최상위 레벨 변수는 바로 사용 가능
 */
fun main(args: Array<String>) {
    val one = T2_One()

    println(threeVal)
    threeFun()
}